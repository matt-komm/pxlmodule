#ifndef _OUTPUTSTORE_H_#define _OUTPUTSTORE_H_#include <unordered_map>#include <TTree.h>#include <TFile.h>#include <TObject.h>#include <TBranch.h>#include <string>#include <iostream>class Tree{    private:        const int INVALID;        int _count;        std::unordered_map<std::string,float*> _variables;        TTree* _tree;        TFile* _file;    public:        Tree(TFile* file, std::string name);        float& getVariable(std::string name);        float* bookVariableAddress(std::string name);        void fill();        void write();};class OutputStore{    private:        TFile* _file;        std::unordered_map<std::string,Tree*> _treeMap;        std::string _defaultTreeName;    public:        OutputStore(std::string filename);        Tree* getBranch(std::string treeName);        void setDefaultTreeName(std::string name)        {            _defaultTreeName=name;        }        void storeValue(const float value, std::string name);        void storeValue(const float value, std::string name, std::string treeName);        void fill();        void close();};#endif