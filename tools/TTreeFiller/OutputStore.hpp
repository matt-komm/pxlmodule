#ifndef _OUTPUTSTORE_H_#define _OUTPUTSTORE_H_#include <unordered_map>#include <TTree.h>#include <TFile.h>#include <TObject.h>#include <TBranch.h>#include <string>#include <iostream>class OutputStore{    private:        const int INVALID;        TFile* file;        std::map<std::string,TTree*> treeMap;        int count;        std::unordered_map<std::string,float*> variables;        TTree* getTree(std::string treeName);        std::string defaultTreeName;        float* getVariableAddress(std::string name, std::string treeName);        float* bookVariableAddress(std::string name, std::string treeName);    public:        OutputStore(std::string filename);        void setDefaultTreeName(std::string treeName);        void storeValue(const float& value, std::string name, std::string treeName);        void storeValue(const float& value, std::string name);        void fill();        void close();};#endif